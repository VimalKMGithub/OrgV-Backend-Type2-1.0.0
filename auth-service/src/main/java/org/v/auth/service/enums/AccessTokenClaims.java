package org.v.auth.service.enums;

public enum AccessTokenClaims {
    DEVICE,
    OS,
    AGENT,
    ACCESS_TOKEN_ID,
    DEVICE_ID,
    USER_ID,
    USERNAME,
    EMAIL,
    REAL_EMAIL,
    AUTHORITIES,
    MFA_ENABLED,
    MFA_METHODS,
    EXPIRATION,
    EMAILS_FROM_EXTERNAL_IDENTITIES,
}
